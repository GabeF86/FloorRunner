export default function SettingsPage() {
  return (
    <div style={{ padding: '32px 40px' }}>
      <h1 style={{ fontSize: 22, fontWeight: 800, color: 'var(--text)', marginBottom: 8 }}>Settings</h1>
      <p style={{ fontSize: 14, color: 'var(--text-muted)' }}>Organization settings, roles, custom fields, holidays, and display configuration — coming soon.</p>
    </div>
  );
}
