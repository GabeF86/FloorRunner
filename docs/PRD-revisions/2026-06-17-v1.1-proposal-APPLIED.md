# PRD Revision Proposal — v1 → v1.1

**Date:** 2026-06-17
**Author:** Agent A14 (PRD Curator) — first run
**Target PRD:** `docs/PRD-Grid-Calculator.md` (currently v1, dated 2026-06-17)
**Status:** **PROPOSAL — awaiting Gabriel approval.** Do NOT apply this revision
to `PRD-Grid-Calculator.md` until Gabriel signs off on the whole document.

This is the inaugural A14 sweep. It inventories every drift from PRD v1 signoff
through Tier 1 + Tier 2 + Tier 3 (A9, A10, A11) shipping. Future sweeps will
diff against the most-recently approved PRD version and stay scoped to the
delta since the previous proposal.

---

## TL;DR

| # | Drift | PRD section | Bucket | Impact |
|---|---|---|---|---|
| 1 | Sanity-band footnote: actual Paoli MD headcount = 14 (not ≥18) | §16 + Changelog | Correctness | High |
| 2 | Solver doesn't honor 4 rule-shape fields the normalizer emits | §17 | Correctness | High |
| 3 | A3 toggle-interaction questions still open | §17 | Open question | Med |
| 4 | A11 seed assumptions (per-diem 0.5 FTE, SRNA = 1.0, ≥18 band) | §17 | Open question | Med |
| 5 | RLS policy bodies deferred to FloorRunner-wide auth pass | §17 | Open question | Med |
| 6 | A10 baseline + audit script + cadence proposal not in PRD | §14 A10 | Charter | Med |
| 7 | Aesthetic checkpoint values locked (Variant C / 160 / 220 / fade-up) | §18 | Decisions log | Low |
| 8 | Tier 1, Tier 2, Tier 3 (A9/A10/A11) shipped 2026-06-17 | §19 | Changelog | Low |
| 9 | A10 finding "MD-heavy" label drift — fixed | §18 | Decisions log | Low |
| ~~10~~ | ~~Patch numbering: PRD §14 A2 should say patch15 not patch14~~ | already correct in PRD v1 | n/a | (skip) |

**Drifts in working list that were verified WRONG and skipped:** 1 (the patch
numbering item — PRD v1 line 260 already says `supabase_scheduling_patch15_provider_leave_buckets.sql` with the parenthetical "(patch14 is reserved for A1)". No edit needed; see Drift 10 note.)

---

## Drift 1 — Sanity-band footnote: actual Paoli MD headcount = 14, not ≥18

- **Source of truth:** `docs/paoli-validation.md` §3.1 (worst-case Anesthesiologist FTE = 14, recommendation = 14) and §6 (A11 escalation explicitly calls out the "charter ≥18 band" as orchestrator drift). PRD §16 acceptance criterion 4 itself says `±2 of current actual headcount` — which is correct as written. The drift is in the orchestrator's working text ("implicit floor `≥18`"), not in the PRD.
- **PRD section affected:** §19 Changelog (add a footnote-style entry). PRD §16 line itself does NOT need surgery.
- **Proposed text:** add to §19 Changelog:

  > - **v1.1 (2026-06-17):** Footnote — for §16 acceptance criterion 4, the "current actual headcount" for the canonical Paoli benchmark is **14 Anesthesiologists** and **30 CRNAs by headcount** (24 FT + 6 per-diem). Earlier orchestrator working text used an implicit floor of ≥18 MD which was a misread of the original PRD intent; `paoli-validation.md` §3.1 confirms 14 is the correct anchor.

- **Why:** Without this, future readers (and future A14 sweeps) will rediscover the same ≥18 vs 14 confusion. The PRD §16 text itself reads fine; the footnote pins the Paoli benchmark number so the next agent can verify ±2 against the right anchor.

---

## Drift 2 — Solver doesn't honor 4 rule-shape fields the normalizer emits

- **Source of truth:** `docs/agent-notes/A4-rules-normalizer.md` §"Open items A3's solver does not yet honor" + `src/lib/gridCalculator/types.ts:112,120,121,122` (fields declared) + `src/lib/gridCalculator/solver.ts` (grepped 0 hits for `globalRules`, `auxiliaryRole`, `maxSupervisionRatio`).
- **PRD section affected:** §17 (Risks & open questions). Add as a new risk bullet.
- **Proposed text:** insert into §17 after the existing "Risk: Simulator overfitting to Paoli." bullet:

  > - **Risk: Solver round-trips rule fields it does not enforce.** A4's normalizer emits per-site `maxSupervisionRatio`, `auxiliaryRole: break_relief | add_on_relief`, top-level `globalRules[]`, and per-site `notes` (declared in `src/lib/gridCalculator/types.ts:112,120-122`). A3's `solver.ts` parses these into the rule set but does NOT consult them when picking supervisors, allocating floats, or selecting room staffing patterns. The free-text → grid pipeline therefore silently drops user-authored rules. Mitigation: file a tracked A3 follow-up to wire each field into the appropriate decision point; meanwhile A4's notes (`docs/agent-notes/A4-rules-normalizer.md` "Open items") are the canonical list of which fields drop.

- **Why:** This is a **correctness bug** in the user-promised flow ("Rules added in plain English; no code change" — PRD §2.4). A hospital writes "OB capped at 1:3" and the grid silently ignores it. Until the PRD acknowledges the gap, A3 has no escalation cue and A11 + reality-check reports look misleadingly clean.

---

## Drift 3 — A3 toggle-interaction questions still open

- **Source of truth:** Orchestrator working list item 4, citing 3 questions from A3 (the A3 agent-notes file isn't present, so these come from A3's PR escalation per the orchestrator). The questions concern solver/toggle/rule interaction edges.
- **PRD section affected:** §17 (Risks & open questions). Add as three sub-bullets.
- **Proposed text:** insert into §17 after Drift 2's bullet:

  > - **Open question (for Gabriel) — solver/toggle interaction edges:**
  >   - When `coverage_style = crna_heavy` combines with `supervision_ratio = mostly_1_3`, what should the solver do at a site whose rules emit `solo_md` as the only staffing pattern? Today the solver picks solo_md (rule wins). Confirm this precedence.
  >   - May the solver use a "solo MD" Anesthesiologist as a **cross-site supervisor** when spare ratio capacity exists? PRD §7.3 mentions cross-site supervision but does not specify whether a nominally "solo" provider can absorb a CRNA at another site.
  >   - PRD §14 A6 owns floats end-to-end. Confirm A6 (Float Strategy) is the sole owner of float positioning decisions, including the bump logic in §12. (A7 already implements this assumption; this question pins it.)

- **Why:** A3 will keep encountering these at the next free-text-guideline corner case. Surfacing them in §17 means the answer is referenced in one place rather than buried in PR comments.

---

## Drift 4 — A11 seed assumptions need confirmation (3 items)

- **Source of truth:** `docs/paoli-validation.md` §6 "GABRIEL ATTENTION" — three explicit confirmations A11 needs.
- **PRD section affected:** §17 (Risks & open questions).
- **Proposed text:** insert into §17 after Drift 3's bullet:

  > - **Open question (for Gabriel) — Paoli seed sanity:** A11's reality-check report (`docs/paoli-validation.md` §6) requests Gabriel confirmation on three numbers that shift the effective-FTE diff:
  >   1. **Per-diem CRNA effective FTE.** Seed defaults the 6 Paoli per-diems to 0.5 FTE; if their actual utilization is closer to 1.0, the +3 CRNA gap closes to 0. If closer to 0.2, it widens to +5 and lands outside §16's ±3 tolerance.
  >   2. **Sanity-band reconciliation.** The A11 charter's "worst-case Anesthesiologist FTE ≥18" was a pre-tuning conservative upper estimate; the simulator landed at 14 (matches Paoli reality). Confirm the band can be retired (A11 already widened the test thresholds to `[9, 30]` in `paoliSeed.test.ts`).
  >   3. **SRNA staffing FTE.** "M. Corbett SRNA" and "S. Peckman SRNA" appear on the working roster and are modeled as 1.0 FTE CRNAs. If SRNAs don't count toward Paoli's production schedule, the CRNA pool is over-counted by 2.

- **Why:** Until these are answered, every future FTE simulation reads "Paoli matches" — even if the underlying assumption is wrong. The §17 entry tells the next A14 sweep to check whether Gabriel answered.

---

## Drift 5 — RLS policy bodies deferred to FloorRunner-wide auth pass

- **Source of truth:** `supabase_scheduling_patch14_grid_calculator.sql:108-112` ("policies to ship in a follow-up patch alongside the FloorRunner auth model") + `supabase_scheduling_patch16_fte_runs.sql:54-55` (same convention).
- **PRD section affected:** §17 (Risks & open questions).
- **Proposed text:** insert into §17 after Drift 4's bullet:

  > - **Open question (for FloorRunner-wide auth pass):** Patches 14 and 16 `ENABLE ROW LEVEL SECURITY` on every grid_calculator table but ship **no policy bodies** — they defer to the FloorRunner-wide auth model (A1 + A7 notes both reference this). Until those policies land, the tables are RLS-locked, which means every backend write must use a service-role connection. Track resolution via the FloorRunner auth roadmap; A14 will note it shipped when patch17 (or equivalent) adds the bodies.

- **Why:** Three tables (`grid_calculator_configs`, `grid_calculator_distances`, `grid_calculator_fte_runs`) currently have RLS enabled with empty policy sets — a backend write attempted from anywhere other than a service-role connection will fail. Future agents touching these tables need to know to use service-role until policies land.

---

## Drift 6 — A10 baseline + audit script + cadence proposal not in PRD §14

- **Source of truth:** `docs/aesthetic-reviews/README.md` (describes the loop), `docs/aesthetic-reviews/baseline.json` (machine-readable constraints), `scripts/aesthetic-audit.ts` (the script). PRD §14 A10 currently mentions only "Markdown report with annotated screenshots in `docs/aesthetic-reviews/`" — silent on the script + baseline.
- **PRD section affected:** §14 (A10 charter).
- **Proposed text:** replace the A10 "Outputs:" line in §14 with:

  > - **Outputs:** Markdown report in `docs/aesthetic-reviews/YYYY-MM-DD-<topic>.md`, machine-readable locked-constraint snapshot at `docs/aesthetic-reviews/baseline.json`, and a static code+DOM audit script at `scripts/aesthetic-audit.ts` (invoked via `npx tsx scripts/aesthetic-audit.ts`; exit code 1 on any `severity: 'error'` finding). Weekly cadence proposed via `/loop 7d /aesthetic-audit` or `/schedule weekly mon 09:00 …`; registration is a Gabriel-side task per the A10 README.

- **Why:** Anyone reading §14 today thinks A10 only emits markdown reports. The audit script is the actual weekly gate; the baseline.json is the source of truth for what "drift" means. Surfacing both in §14 stops future agents from re-inventing the audit pipeline.

---

## Drift 7 — Aesthetic checkpoint values locked (Variant C / 160 / 220 / fade-up shimmer)

- **Source of truth:** `docs/aesthetic-checkpoints/A9-initial.md` line 5 ("Confirmed by Gabriel 2026-06-17 — Variant C locked, lane header 160px locked, card min-width 220px locked, fade-up + blur-out shimmer locked") + `docs/aesthetic-reviews/baseline.json` (codifies the same values).
- **PRD section affected:** §18 (Decisions log).
- **Proposed text:** insert into §18 after the existing "First three agents (A1, A2, A3) dispatched in parallel at PRD signoff." bullet:

  > - **2026-06-17:** Aesthetic baseline locked by Gabriel — **Variant C "Hybrid"** layout, **lane header width = 160px**, **room card min-width = 220px**, shimmer = **200ms fade-up + blur-out**. Canonical baseline in `docs/aesthetic-reviews/baseline.json`; checkpoint rationale in `docs/aesthetic-checkpoints/A9-initial.md`. These values cannot drift without a new approved checkpoint.

- **Why:** §18 is the locked-decision log. A future contributor reaching for `width: 170` or `shimmer: 150ms` needs a visible "Gabriel said no" reference. The baseline.json is the machine check; §18 is the human cue.

---

## Drift 8 — Tier 1, Tier 2, Tier 3 (A9, A10, A11) shipped 2026-06-17

- **Source of truth:** Patch files (14/15/16), `docs/agent-notes/A2,A4,A6,A7-*.md`, `docs/aesthetic-checkpoints/A9-initial.md`, `docs/aesthetic-reviews/2026-06-17-initial.md`, `docs/paoli-validation.md`. PRD §19 currently has only the "v1 (2026-06-17): Initial draft." line.
- **PRD section affected:** §19 (Changelog).
- **Proposed text:** append to §19 (after Drift 1's footnote):

  > - **v1.1 (2026-06-17):** Tier 1 (A1 Site Architect, A2 Provider Profile, A3 Coverage Algorithm) shipped — `patch14_grid_calculator.sql`, `patch15_provider_leave_buckets.sql`, `solver.ts`, `types.ts`, Sidebar. Tier 2 (A4 Rules Normalizer, A5 Distance & Supervisability, A6 Float Strategy, A7 FTE Simulator with watchdog gap closure, A8 Call Burden) shipped — `rulesNormalizer.ts`, `supervisability.ts`, `floatStrategy.ts`, `fteSimulator.ts` + `patch16_fte_runs.sql`, `callBurden.ts`. Tier 3 (A9 Grid Canvas with Variant C layout, A11 Paoli Seed + reality check) shipped; A10 Aesthetic Review framework shipped (baseline + audit script + weekly cadence proposal). See `docs/agent-notes/*.md` and `docs/aesthetic-checkpoints/A9-initial.md` for per-agent ship notes.

- **Why:** PRD §19 is the canonical "what's in the box today" line. A handoff to a new contributor reads §19 first; an empty Changelog after a heavy build day is misleading.

---

## Drift 9 — A10 initial finding: "MD-heavy" label drift — fixed

- **Source of truth:** `docs/aesthetic-reviews/2026-06-17-initial.md` §"Initial findings" (3 error-severity findings, all in `ToggleBar.tsx` lines 32/34). Verified fixed: `grep "MD-heavy\|Anes-heavy"` on `src/app/(scheduling)/grid-calculator/ToggleBar.tsx` returns `Bias toward solo Anesthesiologist vs supervised CRNA rooms` (line 32) and `{ value: 'md_heavy', label: 'Anes-heavy' }` (line 34). The underlying enum value `md_heavy` stays — PRD §5 + §8 explicitly preserves the internal enum.
- **PRD section affected:** §18 (Decisions log).
- **Proposed text:** insert into §18 after Drift 7's bullet:

  > - **2026-06-17:** A10's initial audit caught three "MD" → user-visible label drifts in `ToggleBar.tsx` (PRD §5 + §7.5 forbid user-visible "MD" outside internal enums). A9 patched: `label: 'MD-heavy'` → `label: 'Anes-heavy'` (matches the `ANES` mono badge in `AnesthesiologistCard.tsx`); section description "Bias toward solo MD…" → "Bias toward solo Anesthesiologist…". Internal `value: 'md_heavy'` enum preserved per PRD §5 (internal types may keep `physician`/`md` enums for FloorRunner type compatibility). Audit re-run is expected to exit 0.

- **Why:** §18 captures the why-it-changed; the canonical UI shorthand is now "Anes" (not "Anesthesiologist", not "MD"). Future A9 contributors need to know this is the official short form before they reach for "Phys-heavy" or "Doc-heavy".

---

## Drift 10 — Patch numbering — VERIFIED ALREADY CORRECT IN PRD (skip)

- **Source of truth:** PRD `PRD-Grid-Calculator.md:260` reads:
  > **Outputs:** A new migration `supabase_scheduling_patch15_provider_leave_buckets.sql` (patch14 is reserved for A1), updated TypeScript types in `src/lib/gridCalculator/providerProfile.ts`.

  This already reflects the mid-session fix the orchestrator's working list item 2 wanted to verify. Patch 14 is `supabase_scheduling_patch14_grid_calculator.sql` (A1) and patch 15 is `supabase_scheduling_patch15_provider_leave_buckets.sql` (A2), matching what landed on disk.
- **Proposed text:** none — no PRD edit. The orchestrator's working note can be retired.
- **Why call it out:** the working list flagged this as "verify it stuck and note the fix in the Changelog." It stuck. The Changelog footnote for the v1 → v1.1 bump (Drift 8 above) already names patch14 and patch15 explicitly by file, which is sufficient archival evidence. No standalone Changelog entry needed.

---

## [GABRIEL ATTENTION] — items only Gabriel can resolve

These three open questions are anesthesia-judgment calls that A14 must not
guess. Grouped at the bottom so they're not lost in the proposal.

### G1. Per-diem CRNA effective FTE
- Already covered as Drift 4 sub-bullet #1. The §17 entry surfaces it for future readers; the answer remains a Gabriel decision.
- **What A14 needs from Gabriel:** Pick 0.5 / 1.0 / 0.2 / your own number. A11 will update `seeds/paoli.ts` and re-run.

### G2. ≥18 sanity-band retirement
- Already covered as Drift 4 sub-bullet #2. The simulator landed at 14, matching Paoli reality; A11 has already widened test bands to `[9, 30]`. **Recommendation from A14 (a-priori):** retire the ≥18 band; PRD §16's "±2 of current actual headcount" is the right spec.
- **What A14 needs from Gabriel:** Confirm "retire the ≥18 band" (or rebut with the staffing reasoning A11's §6 sub-bullet (b) outlines).

### G3. SRNAs as 1.0 FTE CRNAs
- Already covered as Drift 4 sub-bullet #3.
- **What A14 needs from Gabriel:** Confirm whether SRNAs count toward Paoli production staffing. If no, the seed drops them and CRNA pool shrinks by 2 → recommendation likely stays at 30 (worst-case bound) but the diff against rostered effective FTE shifts.

---

## Ready-to-apply patch (one-shot diff)

If Gabriel approves the whole revision, the following edits land in
`PRD-Grid-Calculator.md` in this order. Header `## Status` line bumps to
**Draft v1.1** and `**Last updated:** 2026-06-17`.

### Patch block 1 — §14 A10 "Outputs:" line

Replace the existing A10 `**Outputs:**` line (around PRD line 316) with:

```
- **Outputs:** Markdown report in `docs/aesthetic-reviews/YYYY-MM-DD-<topic>.md`, machine-readable locked-constraint snapshot at `docs/aesthetic-reviews/baseline.json`, and a static code+DOM audit script at `scripts/aesthetic-audit.ts` (invoked via `npx tsx scripts/aesthetic-audit.ts`; exit code 1 on any `severity: 'error'` finding). Weekly cadence proposed via `/loop 7d /aesthetic-audit` or `/schedule weekly mon 09:00 …`; registration is a Gabriel-side task per the A10 README.
```

### Patch block 2 — §17 (Risks & open questions) — 4 new bullets

Append to §17 after the existing "Risk: Simulator overfitting to Paoli." bullet:

```
- **Risk: Solver round-trips rule fields it does not enforce.** A4's normalizer emits per-site `maxSupervisionRatio`, `auxiliaryRole: break_relief | add_on_relief`, top-level `globalRules[]`, and per-site `notes` (declared in `src/lib/gridCalculator/types.ts:112,120-122`). A3's `solver.ts` parses these into the rule set but does NOT consult them when picking supervisors, allocating floats, or selecting room staffing patterns. The free-text → grid pipeline therefore silently drops user-authored rules. Mitigation: file a tracked A3 follow-up to wire each field into the appropriate decision point; meanwhile A4's notes (`docs/agent-notes/A4-rules-normalizer.md` "Open items") are the canonical list of which fields drop.
- **Open question (for Gabriel) — solver/toggle interaction edges:**
  - When `coverage_style = crna_heavy` combines with `supervision_ratio = mostly_1_3`, what should the solver do at a site whose rules emit `solo_md` as the only staffing pattern? Today the solver picks solo_md (rule wins). Confirm this precedence.
  - May the solver use a "solo MD" Anesthesiologist as a **cross-site supervisor** when spare ratio capacity exists? PRD §7.3 mentions cross-site supervision but does not specify whether a nominally "solo" provider can absorb a CRNA at another site.
  - PRD §14 A6 owns floats end-to-end. Confirm A6 (Float Strategy) is the sole owner of float positioning decisions, including the bump logic in §12. (A7 already implements this assumption; this question pins it.)
- **Open question (for Gabriel) — Paoli seed sanity:** A11's reality-check report (`docs/paoli-validation.md` §6) requests Gabriel confirmation on three numbers that shift the effective-FTE diff:
  1. **Per-diem CRNA effective FTE.** Seed defaults the 6 Paoli per-diems to 0.5 FTE; if their actual utilization is closer to 1.0, the +3 CRNA gap closes to 0. If closer to 0.2, it widens to +5 and lands outside §16's ±3 tolerance.
  2. **Sanity-band reconciliation.** The A11 charter's "worst-case Anesthesiologist FTE ≥18" was a pre-tuning conservative upper estimate; the simulator landed at 14 (matches Paoli reality). Confirm the band can be retired (A11 already widened the test thresholds to `[9, 30]` in `paoliSeed.test.ts`).
  3. **SRNA staffing FTE.** "M. Corbett SRNA" and "S. Peckman SRNA" appear on the working roster and are modeled as 1.0 FTE CRNAs. If SRNAs don't count toward Paoli's production schedule, the CRNA pool is over-counted by 2.
- **Open question (for FloorRunner-wide auth pass):** Patches 14 and 16 `ENABLE ROW LEVEL SECURITY` on every grid_calculator table but ship **no policy bodies** — they defer to the FloorRunner-wide auth model (A1 + A7 notes both reference this). Until those policies land, the tables are RLS-locked, which means every backend write must use a service-role connection. Track resolution via the FloorRunner auth roadmap; A14 will note it shipped when patch17 (or equivalent) adds the bodies.
```

### Patch block 3 — §18 (Decisions log) — 2 new bullets

Append to §18 after the existing "First three agents…" bullet:

```
- **2026-06-17:** Aesthetic baseline locked by Gabriel — **Variant C "Hybrid"** layout, **lane header width = 160px**, **room card min-width = 220px**, shimmer = **200ms fade-up + blur-out**. Canonical baseline in `docs/aesthetic-reviews/baseline.json`; checkpoint rationale in `docs/aesthetic-checkpoints/A9-initial.md`. These values cannot drift without a new approved checkpoint.
- **2026-06-17:** A10's initial audit caught three "MD" → user-visible label drifts in `ToggleBar.tsx` (PRD §5 + §7.5 forbid user-visible "MD" outside internal enums). A9 patched: `label: 'MD-heavy'` → `label: 'Anes-heavy'` (matches the `ANES` mono badge in `AnesthesiologistCard.tsx`); section description "Bias toward solo MD…" → "Bias toward solo Anesthesiologist…". Internal `value: 'md_heavy'` enum preserved per PRD §5 (internal types may keep `physician`/`md` enums for FloorRunner type compatibility). Audit re-run is expected to exit 0.
```

### Patch block 4 — §19 (Changelog) — bump to v1.1 and add two entries

Append to §19 after the existing "v1 (2026-06-17): Initial draft." line:

```
- **v1.1 (2026-06-17):** Footnote — for §16 acceptance criterion 4, the "current actual headcount" for the canonical Paoli benchmark is **14 Anesthesiologists** and **30 CRNAs by headcount** (24 FT + 6 per-diem). Earlier orchestrator working text used an implicit floor of ≥18 MD which was a misread of the original PRD intent; `paoli-validation.md` §3.1 confirms 14 is the correct anchor.
- **v1.1 (2026-06-17):** Tier 1 (A1 Site Architect, A2 Provider Profile, A3 Coverage Algorithm) shipped — `patch14_grid_calculator.sql`, `patch15_provider_leave_buckets.sql`, `solver.ts`, `types.ts`, Sidebar. Tier 2 (A4 Rules Normalizer, A5 Distance & Supervisability, A6 Float Strategy, A7 FTE Simulator with watchdog gap closure, A8 Call Burden) shipped — `rulesNormalizer.ts`, `supervisability.ts`, `floatStrategy.ts`, `fteSimulator.ts` + `patch16_fte_runs.sql`, `callBurden.ts`. Tier 3 (A9 Grid Canvas with Variant C layout, A11 Paoli Seed + reality check) shipped; A10 Aesthetic Review framework shipped (baseline + audit script + weekly cadence proposal). See `docs/agent-notes/*.md` and `docs/aesthetic-checkpoints/A9-initial.md` for per-agent ship notes.
```

### Patch block 5 — header housekeeping

Replace:

```
**Status:** Draft v1
**Last updated:** 2026-06-17
```

with:

```
**Status:** Draft v1.1
**Last updated:** 2026-06-17
```

---

## Approval checklist (for Gabriel)

- [ ] Drift 1 footnote (sanity-band anchor = 14, not ≥18) — approve / edit / reject.
- [ ] Drift 2 §17 entry (solver doesn't honor 4 rule-shape fields) — approve / edit / reject.
- [ ] Drift 3 §17 entry (3 solver/toggle interaction questions) — approve / edit / reject.
- [ ] Drift 4 §17 entry (3 Paoli seed sanity questions) — approve / edit / reject.
- [ ] Drift 5 §17 entry (RLS bodies deferred) — approve / edit / reject.
- [ ] Drift 6 §14 A10 charter rewrite (baseline + script + cadence) — approve / edit / reject.
- [ ] Drift 7 §18 entry (aesthetic baseline lock) — approve / edit / reject.
- [ ] Drift 8 §19 entry (Tier 1/2/3 ship log) — approve / edit / reject.
- [ ] Drift 9 §18 entry (Anes-heavy label fix) — approve / edit / reject.
- [ ] G1 / G2 / G3 [GABRIEL ATTENTION] — answer in line, A14 will roll the answers into v1.2 + flag downstream agents.

Once approved, Gabriel applies the patch blocks in order. A14 then renames this file with a `-APPLIED` suffix and starts the v1.2 proposal for the next sweep.
